<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fr.iutmindfuck.qcmiutlyon1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        
        <activity android:name=".activity.TeacherPanelActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activity.MCQEditionActivity"/>
        <activity android:name=".activity.MCQListActivity" />
        <activity android:name=".activity.QuestionEditionActivity" />
        <activity android:name=".activity.AuthActivity" />
        <activity android:name=".activity.QuestionListActivity" />
    </application>

</manifest>